# ComfyUI CY Nodes 🍌

基于 Gemini API 的 ComfyUI 图像生成和编辑节点，支持文生图、图片编辑、批量生成等功能。

![节点预览](https://img.shields.io/badge/ComfyUI-Custom_Nodes-blue)
![版本](https://img.shields.io/badge/version-1.0.0-green)

---

## ✨ 功能特点

- 🎨 **文生图** - 通过文字描述生成高质量图片
- 🖼️ **图片编辑** - 对现有图片进行 AI 编辑和修改（支持最多8张参考图）
- 🔄 **批量生成** - 支持同时生成多张图片（最多10张），提高效率
- 📝 **批量提示词** - 按行拆分提示词，每行独立生成不同图片
- 🔑 **自动创建令牌** - 一键生成临时 API Key，保护主 Key 安全
- 💾 **API Key 自动保存** - 首次输入后自动保存，无需重复填写
- 📐 **多种宽高比** - 支持 1:1、4:3、3:4、16:9、9:16、2:3、3:2、21:9、4:5、5:4
- 🎯 **多种尺寸** - 支持 1K、2K、4K 等多种分辨率
- 🎲 **随机种子** - 支持设置种子复现结果，或自动随机

---

## 📦 包含节点

| 节点名称 | 类名 | 功能说明 |
|---------|------|---------|
| **CY文生图** | `CYTextToImage` | 文字描述 → 生成图片（支持批量） |
| **CY图片编辑** | `CYImageEdit` | 输入图片 + 描述 → 编辑后的图片（支持8输入5输出） |
| **CY保存图片** | `CYSaveImage` | 保存生成的图片到本地 |

---

## 🚀 安装方法

### 方式一：通过 ComfyUI Manager（推荐）

1. 打开 ComfyUI Manager
2. 搜索 `CY Nodes` 或 `ComfyUI_CY_Nodes`
3. 点击 **Install** 安装
4. 重启 ComfyUI

### 方式二：Git 克隆

```bash
cd ComfyUI/custom_nodes
git clone https://github.com/XY121718/ComfyUI_CY_Nodes.git
```

### 方式三：下载 ZIP

1. 点击 GitHub 页面的 **Code → Download ZIP**
2. 解压到 `ComfyUI/custom_nodes/ComfyUI_CY_Nodes`
3. 重启 ComfyUI

> ⚠️ **无需安装额外依赖**，所有库都是 ComfyUI 自带的！

---

## 📖 使用教程

### 🔑 第一步：获取 API Key

你需要一个支持 Gemini API 的中转服务 Key：

| 中转服务 | 网址 | 特点 |
|---------|------|------|
| **星核AI** | https://api.xheai.cc | ✅ 支持自动创建令牌 |
| **aicy168** | https://ai.aicy168.top | 备用中转 |

---

### 🎨 第二步：使用 CY文生图（基础用法）

1. 在节点面板搜索 `CY文生图` 并添加到画布
2. **中转网址**：选择你的 API 服务商
3. **Key1**（必填）：填入你的 API Key
4. **提示词**：描述你想生成的图片，例如：
   ```
   一只可爱的橘猫，坐在窗台上，阳光照射，高清摄影
   ```
5. **模型**：选择使用的 AI 模型
   - `gemini-3-pro-image-preview` - Gemini 官方模型
   - `nano-banana` - 优化模型
   - `nano-banana-2` - 优化模型 v2
   - `nano-banana-2-2k` - 2K 分辨率优化
   - `nano-banana-2-4k` - 4K 分辨率优化
6. **默认宽高比**：选择图片比例（1:1、4:3、16:9 等）
7. **图像尺寸**：选择分辨率（1K、2K、4K）
8. 连接输出到 **预览图片** 节点
9. 点击 **Queue Prompt** 开始生成

---

### 🚀 第三步：批量生成（高级用法）

#### 方式一：同时生成多张（生成张数）

1. 设置 **生成张数** 为 2-10
2. 系统会并行生成多张图片，速度更快
3. 所有图片使用相同的提示词

> 💡 生成张数越大，速度越快，但消耗更多 API 配额

#### 方式二：不同提示词生成不同图片（按行拆分）

1. 开启 **按行拆分提示词**
2. 在提示词框中，用**两次回车**分隔不同的描述：
   ```
   一只可爱的橘猫

   一只黑色的小狗

   一只白色的兔子
   ```
3. 每行提示词会独立生成一张图片

> ⚠️ 注意：生成张数 > 1 时，不能同时开启按行拆分

---

### 🖼️ 第四步：使用 CY图片编辑

CY图片编辑 可以对现有图片进行 AI 编辑，支持最多 **8张输入图片** 和 **5张输出图片**。

#### 基础用法：单图编辑

1. 在节点面板搜索 `CY图片编辑` 并添加到画布
2. 填写 **Key1**（必填）
3. 将要编辑的图片连接到 **输入1**
4. 在 **提示词** 中描述你想要的修改，例如：
   ```
   把背景改成海边，添加夕阳效果
   ```
5. 运行即可获得编辑后的图片

#### 高级用法：多图合成/参考编辑 🔥

**核心功能**：可以输入多张图片作为参考，AI 会综合所有输入图片的内容进行编辑！

**使用场景举例：**

1. **风格迁移**：输入1放原图，输入2放风格参考图
   ```
   将图1的内容用图2的风格重新绘制
   ```

2. **换脸/换装**：输入1放人物照片，输入2放目标服装
   ```
   把图1中的人物穿上图2中的衣服
   ```

3. **场景合成**：输入1放人物，输入2放背景
   ```
   把图1中的人物放到图2的场景中
   ```

4. **多图融合**：输入多张图片，生成综合效果
   ```
   融合所有输入图片的元素，创作一张新图
   ```

#### 多图编辑操作步骤：

1. 将 **主图片** 连接到 **输入1**
2. 将 **参考图片** 连接到 **输入2、输入3...** （最多8张）
3. 在提示词中说明如何使用这些图片，例如：
   ```
   参考图2的色调，将图1重新上色
   ```
4. 运行后，**输出1-5** 会生成不同的编辑结果

#### 图片编辑特有功能：

| 参数 | 说明 |
|------|------|
| **输入1-8** | 可连接最多8张参考图片，AI 会综合分析所有图片 |
| **输出1-5** | 最多输出5张不同的编辑结果 |
| **匹配参考尺寸** | 开启后，输出图片保持输入1的尺寸 |
| **按行拆分提示词** | 多行提示词分别对应不同的编辑任务 |


---

### ✅ 第五步：自动创建令牌（推荐！）

如果你使用 **api.xheai.cc** 中转服务，强烈建议使用自动创建令牌功能：

1. **中转网址** 选择 `https://api.xheai.cc`
2. 在 **总Key（选填，用于自动生成Key1）** 中填入你的主 Key
3. 点击 **✅ 点击自动创建令牌** 按钮
4. 系统会自动生成一个临时 Key 并填入 Key1

**为什么要用这个功能？**
- 🔒 **更安全**：临时 Key 用完即弃，不怕泄露
- 💰 **更可控**：可以限制临时 Key 的配额
- 🔄 **更方便**：一键生成，无需手动复制粘贴

---

## ⚙️ 完整参数说明

### CY文生图 参数

| 参数名 | 类型 | 默认值 | 说明 |
|-------|------|--------|------|
| 提示词 | 文本 | - | 描述你想生成的图片内容 |
| 中转网址 | 下拉框 | api.xheai.cc | 选择 API 中转服务地址 |
| 总Key | 文本 | - | 用于自动创建令牌（选填） |
| Key1 | 文本 | - | API Key（必填） |
| 模型 | 下拉框 | gemini-3-pro-image-preview | 选择使用的 AI 模型 |
| 默认宽高比 | 下拉框 | 4:3 | 1:1, 4:3, 3:4, 16:9, 9:16, 2:3, 3:2, 21:9, 4:5, 5:4 |
| 图像尺寸 | 下拉框 | 1K | 1K, 2K, 4K |
| 生成张数 | 数字 | 1 | 同时生成的图片数量 (1-10) |
| 按行拆分提示词 | 开关 | 关闭 | 开启后每行提示词独立生成 |
| 种子 | 数字 | 随机 | 随机种子，用于复现结果 |
| 生成后控制 | 下拉框 | randomize | 生成后种子的处理方式 |

### CY图片编辑 参数

| 参数名 | 类型 | 默认值 | 说明 |
|-------|------|--------|------|
| 输入1-8 | 图像 | - | 要编辑的原始图片（支持多张） |
| 提示词 | 文本 | - | 描述你想要的编辑效果 |
| 中转网址 | 下拉框 | api.xheai.cc | 选择 API 中转服务地址 |
| 总Key | 文本 | - | 用于自动创建令牌（选填） |
| Key1 | 文本 | - | API Key（必填） |
| 模型 | 下拉框 | gemini-3-pro-image-preview | 选择使用的 AI 模型 |
| 默认宽高比 | 下拉框 | 4:3 | 输出图片的宽高比 |
| 图像尺寸 | 下拉框 | 1K | 输出图片的分辨率 |
| 匹配参考尺寸 | 开关 | 关闭 | 输出图片是否保持原图尺寸 |
| 按行拆分提示词 | 开关 | 关闭 | 多行提示词分别处理 |
| 种子 | 数字 | 随机 | 随机种子 |

---

## 🎯 支持的模型

| 模型名称 | 说明 | 推荐场景 |
|---------|------|---------|
| `gemini-3-pro-image-preview` | Gemini 官方模型 | 通用场景 |
| `nano-banana` | 优化模型 | 快速生成 |
| `nano-banana-2` | 优化模型 v2 | 质量更好 |
| `nano-banana-2-2k` | 2K 优化模型 | 高清图片 |
| `nano-banana-2-4k` | 4K 优化模型 | 超高清图片 |

---

## 📐 支持的宽高比

| 比例 | 适用场景 |
|------|---------|
| 1:1 | 头像、图标、正方形构图 |
| 4:3 | 传统照片、显示器 |
| 3:4 | 竖版照片 |
| 16:9 | 视频封面、桌面壁纸 |
| 9:16 | 手机壁纸、短视频 |
| 2:3 / 3:2 | 摄影照片 |
| 21:9 | 超宽屏、电影画面 |
| 4:5 / 5:4 | Instagram 等社交媒体 |

---

## 🔧 配置文件

首次使用时，节点会自动创建以下配置文件（无需手动创建）：

| 文件 | 说明 |
|------|------|
| `config.ini` | 保存 API Key |
| `master_key.ini` | 保存总 Key |

> ⚠️ 这些文件包含敏感信息，请勿分享！

---

## ❓ 常见问题

### Q: 提示 "请在总Key随便填入一个有效Key"
A: 点击自动创建令牌前，需要先在"总Key"输入框填入你的主 API Key。

### Q: 生成失败，提示 401 错误
A: API Key 无效或已过期，请检查 Key1 是否正确填写。

### Q: 生成张数设置大于1后报错
A: 确保没有同时开启"按行拆分提示词"，这两个功能不能同时使用。

### Q: 如何复现相同的图片？
A: 记录生成时的种子值，下次使用相同的种子和提示词即可复现。

### Q: 如何更新节点？
A: 通过 ComfyUI Manager 点击 Update，或在命令行执行 `git pull`。

---

## 📄 许可证

MIT License

---

## 🙏 致谢

- [ComfyUI](https://github.com/comfyanonymous/ComfyUI)
- [Google Gemini API](https://ai.google.dev/)
- [星核AI](https://api.xheai.cc)
